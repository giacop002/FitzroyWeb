# Copy this file to .env and fill in values as needed.
# Only PUBLIC_* variables are exposed to the browser.

# Optional: URL of an embeddable stock quote widget (iframe src).
# If not set, /investors/stock shows a placeholder with links to TSX-V and OTC.
# PUBLIC_STOCK_WIDGET_URL=https://your-widget-provider.com/embed/FTZ
